## Backlog and outcome

- Backlog ID:
- User-visible outcome:
- Intentionally out of scope:

## Changes

-

## Verification

- [ ] `./scripts/verify.cmd -BaseRef origin/main` (or `verify.ps1` in CI)
- [ ] Changed behavior was exercised at the appropriate unit, widget, workflow,
      or device level.
- [ ] UI changes include screenshots or a short explanation of why they do not.
- [ ] Database changes use a new migration and include authorization/RLS review.

Commands and results:

```text

```

## Isolation and safety

- [ ] Only `Gr-linkk/vortice-app-next` was used as a Git remote.
- [ ] Any Supabase work targeted only Vortice Next (`hkjpojobdbbtjkhaudki`).
- [ ] No credentials, local configuration, `work/`, or `outputs/` files are
      included.

## Follow-up

- Known limitations or separately tracked work:
