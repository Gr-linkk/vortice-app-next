name: Flutter CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: flutter-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  database-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Verify all database contracts and isolated restore
        shell: bash
        run: |
          python3 scripts/test_operations_tools.py
          bash scripts/test-database.sh --restore-drill

  work-hub-performance:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Check populated Work hub scope and performance
        shell: bash
        run: bash scripts/check-work-hub-performance.sh

  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up PowerShell
        shell: bash
        run: command -v pwsh

      - name: Set up Flutter
        uses: subosito/flutter-action@v2.23.0
        with:
          flutter-version: 3.44.0
          channel: stable
          cache: true

      - name: Verify repository
        shell: pwsh
        run: |
          $baseRef = if ('${{ github.event_name }}' -eq 'pull_request') {
            '${{ github.event.pull_request.base.sha }}'
          } else {
            ''
          }
          ./scripts/verify.ps1 -BaseRef $baseRef
